apiVersion: v1
kind: Service
metadata:
  name: webapp-svc
  namespace: web
spec:
  type: LoadBalancer           # or NodePort/ClusterIP as needed
  selector:
    app: webapp
  ports:
    - port: 80
      targetPort: 3000
